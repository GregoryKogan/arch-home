#!/usr/bin/env python3

import sys

from src.ConfigBuilder import ConfigBuilder

if len(sys.argv) < 2:
    print("You must specify entry config file.")
    print(f"Usage: {sys.argv[0]} <path/to/host.toml>")
    sys.exit(1)

conf_builder = ConfigBuilder(sys.argv[1])
print(conf_builder.build())
